syntax = "proto3";

package mmo.login;

// 客户端 -> 登录服
message C2L_LoginReq {
  string account = 1;     // 账号
  string password = 2;    // 密码
}

// 登录服 -> 客户端
message L2C_LoginRsp {
  bool ok = 1;
  uint64 player_id = 2;   // 玩家ID
  uint64 session_id = 3;  // 登录id
  uint32 gateway_ip = 4;  // 网关ID
  string reason = 7;      // 失败原因
}